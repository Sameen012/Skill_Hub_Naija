services:
  - type: static
    name: skill-hub-naija-client
    runtime: static
    staticPublishPath: client/dist
    buildCommand: cd client && npm install && npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html